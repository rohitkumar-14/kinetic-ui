---
title: Badge
description: Small status badges used for labels, pill indicators, and custom counts.
category: Data Display
---

The `Badge` component renders a small label indicator. Supports standard variant styles as well as a neon gradient premium variant.

## Live Preview

<ComponentPreview 
  name="Badge" 
  componentName="Badge"
  sourceFile="components/ui/badge.tsx"
  playgroundControls={[
    { name: "variant", type: "select", options: ["default", "secondary", "destructive", "outline", "premium", "glow"], default: "default", label: "Variant" }
  ]}
  propsData={[
    { name: "variant", type: "'default' | 'secondary' | 'destructive' | 'outline' | 'premium'", default: "'default'", description: "Visual style variant of the badge." }
  ]}
  variants={[
    {
      name: "Default",
      code: `import { Badge } from "@/components/ui/badge";\n\nexport default function Example() {\n  return (\n    <Badge>Active Node</Badge>\n  );\n}`
    },
    {
      name: "Secondary",
      code: `import { Badge } from "@/components/ui/badge";\n\nexport default function Example() {\n  return (\n    <Badge variant="secondary">Archive</Badge>\n  );\n}`
    },
    {
      name: "Destructive",
      code: `import { Badge } from "@/components/ui/badge";\n\nexport default function Example() {\n  return (\n    <Badge variant="destructive">Deprecated</Badge>\n  );\n}`
    },
    {
      name: "Outline",
      code: `import { Badge } from "@/components/ui/badge";\n\nexport default function Example() {\n  return (\n    <Badge variant="outline">Draft Mode</Badge>\n  );\n}`
    },
    {
      name: "Premium",
      code: `import { Badge } from "@/components/ui/badge";\nimport { Sparkles } from "lucide-react";\n\nexport default function Example() {\n  return (\n    <Badge variant="premium" className="flex items-center gap-1">\n      <Sparkles className="w-3 h-3 text-yellow-300" />\n      Enterprise\n    </Badge>\n  );\n}`
    },
    {
      name: "Glow Effect",
      code: `import { Badge } from "@/components/ui/badge";\nimport { Activity } from "lucide-react";\n\nexport default function Example() {\n  return (\n    <Badge className="border-indigo-500/30 bg-indigo-500/10 text-indigo-300 shadow-[0_0_15px_rgba(99,102,241,0.25)]">\n      <Activity className="w-3 h-3 animate-pulse" />\n      Live Stream\n    </Badge>\n  );\n}`
    }
  ]}
>
  <BadgeDemo variant="default" />
  <BadgeDemo variant="secondary" />
  <BadgeDemo variant="destructive" />
  <BadgeDemo variant="outline" />
  <BadgeDemo variant="premium" />
  <BadgeDemo variant="glow" />
</ComponentPreview>

## Installation

<CliInstallCommand componentName="badge" />

