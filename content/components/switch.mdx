---
title: Switch
description: A control that allows the user to toggle between checked and not checked.
category: Forms
---

A control that allows the user to toggle between checked and not checked states.

## Live Preview

<ComponentPreview 
  name="Switch" 
  componentName="Switch"
  sourceFile="components/ui/switch.tsx"
  playgroundControls={[
    { name: "disabled", type: "boolean", default: false, label: "Disabled" }
  ]}
  propsData={[
    { name: "Inherits Props", type: "Radix UI", default: "—", description: "Inherits all standard properties from @radix-ui/react-switch." }
  ]}
  variants={[
    {
      name: "Default",
      code: `import { Switch } from "@/components/ui/switch"\nimport { Label } from "@/components/ui/label"\n\nexport default function Example() {\n  return (\n    <div className="flex items-center space-x-2">\n      <Switch id="airplane-mode" />\n      <Label htmlFor="airplane-mode">Airplane Mode</Label>\n    </div>\n  );\n}`
    }
  ]}
>
  <SwitchDemo />
</ComponentPreview>

## Installation

<CliInstallCommand componentName="switch" />

